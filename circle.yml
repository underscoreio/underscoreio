machine:
  timezone:
    Europe/London
  node:
    version: "v5.7.0"
  # ruby:
  #   version: "2.2.4p230" # this is the default on CircleCI Ubuntu 14.04
  # python:
  #   version: "2.7.11"    # this is the default on CircleCI Ubuntu 14.04

dependencies:
  override:
    - npm install -g yarn
    - yarn
    - bundle install
    - sudo pip install awscli

test:
  override:
    - bundle exec rake test

deployment:
  # production:
  #   branch: master
  #   owner: underscoreio
  #   commands:
  #     - bundle exec rake deploy-production
  staging:
    branch: [develop, feature/v4]
    owner: underscoreio
    commands:
      - bundle exec rake deploy-beta
